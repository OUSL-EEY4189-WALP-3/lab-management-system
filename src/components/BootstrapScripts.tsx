'use client';
import { useEffect } from 'react';
import 'bootstrap/dist/js/bootstrap.bundle.min.js';

export default function BootstrapScripts() {
  useEffect(() => {
    // JS only runs in the browser
  }, []);
  return null;
}
